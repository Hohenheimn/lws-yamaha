const config = {
  google: {
    mapApiKey: process.env.GMAP_API_KEY,
  },
  imageBaseUrl: process.env.IMAGE_BASE_URL,
  apiBaseUrl: process.env.API_BASE_URL,
  apiNextBaseUrl: process.env.API_NEXT_BASE_URL,
};

export default config;
